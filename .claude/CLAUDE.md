# Aikami — Claude Code entry point

`AGENTS.md` at the repo root is the single source of truth for agent
guidance. Skills live in `.pi/skills/` (exposed to Claude Code through the
`.claude/skills` symlink). Do not add guidance here — edit `AGENTS.md` or the
relevant skill instead.

@../AGENTS.md
