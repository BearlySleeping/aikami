# Archived Contracts

These 120 contracts are legacy v1 artifacts. They were marked completed but
contain no execution report, and are not referenced as dependencies by any
pending work in `docs/TODO.md`.

They remain in the repository for historical reference. The canonical active
contracts live in `docs/contracts/`.

To search archived contracts:
```bash
rg "C-XXX" docs/contracts/archived/
```
