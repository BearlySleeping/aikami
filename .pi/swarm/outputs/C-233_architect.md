Plan: Align existing C-233 implementation (ViewModel, View, schemas, seeding, tests, sandbox, POM, E2E/visual suites) with contract spec — update 8 data model types, refactor `assembleGmPrompt` to standalone data-layer function, add `language`/`additionalPreferences` fields and GM-secret output sections, then validate via `moon run client:fix && moon run client:typecheck && moon run client:test && cd apps/e2e && bun run test && bun run test:visual`.