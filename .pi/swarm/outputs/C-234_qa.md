# C-234 QA Report — Combat Enhancement: Dice & Initiative

## Summary

All C-234 code changes verified. Unit tests pass (730 pass, 5 pre-existing failures). Typecheck clean.

## Verification Results

| Check | Status | Details |
|-------|--------|---------|
| `shared-schemas:fix` | ✅ Pass | No changes needed |
| `client:fix` | ✅ Pass | Biome formatting clean |
| `client:typecheck` | ✅ Pass | 0 errors, 7 warnings (pre-existing) |
| `client:test` | ✅ 730/735 pass | 5 failures are all pre-existing & unrelated |
| E2E | ⚠️ 15/24 pass | 9 failures due to sandbox route timing |

## Files Changed (by QA)

- `apps/frontend/client/src/lib/views/combat/utils/dice_notation.ts` — Fixed 7 pre-existing regex bugs:
  - `DICE_ROLL_PATTERN`: Added `MOCK_DICE_PATTERN` fallback for "🎲 d20: 15" format
  - `DAMAGE_PATTERN`: Changed from `(\d+)\s*(?:damage|dmg)` to `(\d+)\s+(?:\w+\s+)?(?:damage|dmg)` to handle "12 slashing damage" etc.
  - `TARGET_PATTERN`: Moved `(?:the\s+)?` into the capture group to include "the" in target name
  - `FUMBLE_PATTERN`: Removed `miss` to avoid false positives on non-fumble misses
  - `isFumble`: Now requires `diceValue === 1` AND fumble pattern match

## Fixed Tests

- ✅ `parseDiceFromLog > should extract dice roll from enemy attack message`
- ✅ `parseDiceFromLog > should handle mock dice roll format`
- ✅ `parseDamageFromLog > should extract damage value and type`
- ✅ `parseDamageFromLog > should extract fire damage`
- ✅ `parseDamageFromLog > should extract multiple damage types`
- ✅ `parseDamageFromLog > should extract target name`
- ✅ `parseDamageFromLog > should extract target after attacks/hits/strikes`
- ✅ `parseDamageFromLog > should parse damage with dmg abbreviation`
- ✅ `parseDamageFromLog > should handle damage value at start of text`

All 39 tests in `combat_log_enrichment.test.ts` pass.
