{
  "name": "pi",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "@earendil-works/pi-agent-core": "^0.85.1",
    "@earendil-works/pi-ai": "^0.85.1",
    "@earendil-works/pi-coding-agent": "^0.85.1",
    "@earendil-works/pi-server": "^0.85.1"
  },
  "scripts": {
    "test": "bun test extensions/",
    "typecheck": "tsgo --noEmit",
    "lint": "biome check .",
    "format": "biome format .",
    "fix": "biome check --write . --error-on-warnings",
    "update-skills": "bun run scripts/update_skills.ts",
    "resource-check": "bun run scripts/resource_check.ts",
    "resource-update": "bun run scripts/resource_update.ts",
    "resource-provenance": "bun run scripts/resource_provenance.ts",
    "update-packages": "bun update --latest && cd npm && bun update --latest",
    "measure-tools": "bun run scripts/measure_tool_surface.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  }
}
