{
  "name": "pi",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@earendil-works/pi-coding-agent": "^0.80.3"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "lint": "biome check .",
    "format": "biome format .",
    "fix": "biome check --write . --error-on-warnings",
    "update-skills": "bun run scripts/update_skills.ts",
    "update-packages": "cd npm && bun update --latest"
  },
  "dependencies": {
    "@hypabolic/hypa": "^0.1.7"
  }
}
