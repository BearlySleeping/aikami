Remaining known gaps (flagged earlier, intentionally not in this batch): git push branch
guard (push -u origin HEAD, refuse on main), spawnSync blocking the director loop during QA
(+ invisible qa pane), scratchpad double-instantiation, and deleting the vestigial
TaskPayload.steps field. The one thing no test can replace: run one contract end-to-end
through feedback → lgtm with a human at the keyboard.
