{
  "_comment": "Reviewed exceptions to the source-file-size hard limit. Each entry needs an exact maxLines, a rationale, an owner, and a kind. Temporary debt (kind \"other\") also needs an issue or reviewBy date. See .pi/skills/aikami-conventions/SKILL.md. Remove exceptions as files shrink; the guard fails on obsolete entries.",
  "packages/shared/constants/src/lib/country_codes.ts": {
    "maxLines": 6844,
    "rationale": "Cohesive ISO-3166 country/region table: one nested literal per country, consumed as a single lookup dataset. Splitting it would scatter one data structure across files without creating a responsibility boundary.",
    "owner": "@aikami/platform",
    "kind": "declarative"
  },
  "packages/shared/constants/src/lib/locale_codes.ts": {
    "maxLines": 1658,
    "rationale": "Cohesive locale registry (one record per locale) consumed as a single lookup table. It is declarative data with no logic; file boundaries would add imports, not cohesion.",
    "owner": "@aikami/platform",
    "kind": "declarative"
  },
  "packages/shared/lpc/src/lib/legacy_catalog_snapshot.ts": {
    "maxLines": 1240,
    "rationale": "Generated legacy LPC catalog ordering snapshot (DO NOT hand-edit; regenerated from the test fixture and kept in parity by a test). A single frozen data structure that must not be split because legacy appearance indices depend on its exact order.",
    "owner": "@aikami/lpc",
    "kind": "generated"
  },
  "packages/shared/utils/src/lib/form/weak_passwords.ts": {
    "maxLines": 3347,
    "rationale": "Cohesive static password blocklist (top 10000 common passwords, filtered). It is a single reference dataset with no logic; splitting adds imports without creating a responsibility boundary.",
    "owner": "@aikami/platform",
    "kind": "declarative"
  },
  "apps/frontend/client/src/lib/services/game/session_service.svelte.ts": {
    "maxLines": 1007,
    "rationale": "One cohesive session-lifecycle service (sessions, recaps, checkpoint CRUD, fork, context compaction). A review-mandated compensating fork saga grew it 21 lines past its grandfathered baseline; the saga and its rollback belong with the checkpoint transitions it guards, so the growth is accepted rather than split across files.",
    "owner": "@aikami/platform",
    "kind": "kernel"
  },
  "apps/frontend/client/src/lib/views/dev/obsidian/obsidian_view_model.svelte.ts": {
    "maxLines": 809,
    "rationale": "Fixture-driven design-sandbox ViewModel (Phase 1). It reached main at 809 lines via the #327 squash while the PR head measured 800; main's push-to-main guard run does not re-scan unchanged files, so the oversize surfaced only on the next apps-touching PR. Reviewed so this provenance PR can land; the sandbox is dev-only and slated for removal once the design is proven.",
    "owner": "@aikami/client",
    "kind": "other",
    "reviewBy": "2026-12-31"
  },
  "scripts/src/lib/agents/contract_pipeline.ts": {
    "maxLines": 1193,
    "rationale": "Grew on main in e8080d511 (\"fix: contract pipeline\"), which changed this file but did not re-run guard_source_file_size. Main's push-to-main run has an empty affected set, so it never re-scans and the oversize surfaced only on the next scripts-touching PR (C-511). Accepted so that PR can land. This is the pipeline's public surface: every exported stage entry point and its shared run context live here.",
    "owner": "@aikami/platform",
    "kind": "other",
    "reviewBy": "2026-12-31"
  },
  "scripts/src/lib/agents/contract_pipeline/orchestrator.ts": {
    "maxLines": 2335,
    "rationale": "Grew on main in e8080d511 (\"fix: contract pipeline\"), which changed this file but did not re-run guard_source_file_size. Main's push-to-main run has an empty affected set, so it never re-scans and the oversize surfaced only on the next scripts-touching PR (C-511). Accepted so that PR can land. The stage state machine (planning -> implement -> verify -> review -> reconcile). All stages share one mutable run context and transition table, so splitting it would spread a single state machine across files without creating a boundary.",
    "owner": "@aikami/platform",
    "kind": "other",
    "reviewBy": "2026-12-31"
  },
  "scripts/src/lib/herdr/session.ts": {
    "maxLines": 2279,
    "rationale": "Grew on main in e8080d511 (\"fix: contract pipeline\"), which changed this file but did not re-run guard_source_file_size. Main's push-to-main run has an empty affected set, so it never re-scans and the oversize surfaced only on the next scripts-touching PR (C-511). Accepted so that PR can land. herdr session bookkeeping (service defs, engine probes, port allocation). C-511 extracts the engine-probe factory into services/engine_probe.ts and shrinks this file; the entry is time-boxed so the ratchet gets restored rather than forgotten.",
    "owner": "@aikami/platform",
    "kind": "other",
    "reviewBy": "2026-12-31"
  },
  "scripts/src/lib/herdr/worktree.ts": {
    "maxLines": 1323,
    "rationale": "Grew on main in e8080d511 (\"fix: contract pipeline\"), which changed this file but did not re-run guard_source_file_size. Main's push-to-main run has an empty affected set, so it never re-scans and the oversize surfaced only on the next scripts-touching PR (C-511). Accepted so that PR can land. herdr worktree lifecycle (create/checkpoint/reconcile/teardown); these paths mutate one worktree registry and must stay ordered together.",
    "owner": "@aikami/platform",
    "kind": "other",
    "reviewBy": "2026-12-31"
  }
}
