# packages/shared/schemas/moon.yml
$schema: 'https://moonrepo.dev/schemas/project.json'

language: 'typescript'
layer: 'library'
tags:
  - 'schemas'
  - 'validation'
  - 'shared'
  - 'library'

project:
  name: 'schemas'
  description: 'Shared Zod schemas for data validation across the monorepo.'
  channel: '#general'
  owner: 'Core Team'

dependsOn:
  - 'constants'

fileGroups:
  sources:
    - 'src/**/*'
  configs:
    - 'package.json'
    - 'tsconfig.json'
  tests:
    - 'tests/**/*'

tasks:
  test:
    command: 'bun run test'
    inputs:
      - '@group(sources)'
      - '@group(tests)'
      - '@group(configs)'
