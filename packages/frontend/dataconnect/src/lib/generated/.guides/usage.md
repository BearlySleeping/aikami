# Usage

Import from the parent package (`@aikami/frontend-dataconnect`):

```ts
import { dataConnect, connectorConfig } from '@aikami/frontend-dataconnect';
```

The `dataConnect` instance is pre-configured with emulator support in
development mode. Use it directly with generated query refs.
