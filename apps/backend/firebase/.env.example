# Backend
GEMINI_API_KEY=
FIREBASE_SERVICE_ACCOUNT=
APP_ID=firebase
MODE=emulator

REDIS_URL=
REDIS_TOKEN=

# NOTE: the Discord Interactions Endpoint (/ask) moved to
# apps/backend/worker + packages/backend/discord-bot (docs/contracts/
# C-418-p2, OQ-3) — its env keys (DISCORD_PUBLIC_KEY, GITHUB_ISSUES_TOKEN,
# OPENROUTER_API_KEY, OPENROUTER_MODEL) live in apps/backend/worker/.env.example
# now, not here.
