# ============================================================
# Voice Microservice — Headless Kokoro TTS Engine
# Uses the official hwdsl2/kokoro-server:latest image.
# Internally serves on port 8880; Docker maps to host 8089.
# ============================================================

FROM hwdsl2/kokoro-server:latest
EXPOSE 8880
