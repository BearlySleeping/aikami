# apps/backend/voice/moon.yml
$schema: 'https://moonrepo.dev/schemas/project.json'

language: 'typescript'
layer: 'application'
tags:
  - 'voice'
  - 'backend'
  - 'application'
  - 'container'

project:
  name: 'voice'
  description: 'Local sherpa-onnx voice synthesis engine via the local-stack compose topology (C-392) — container-only, no Bun source.'
  channel: '#backend'
  owner: 'Backend Team'

dependsOn: []

fileGroups:
  configs:
    - 'package.json'

tasks:
  build:
    command: 'true'

  dev:
    command: 'bun run dev:docker'
    preset: 'server'

  test:
    command: 'bun run test'
