import type * as firebase from 'firebase/compat';

export type CollectionReference = firebase.default.firestore.CollectionReference;
export type DocumentReference = firebase.default.firestore.DocumentReference;
