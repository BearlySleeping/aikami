# apps/backend/image/moon.yml
$schema: 'https://moonrepo.dev/schemas/v2/project.json'

language: 'typescript'
layer: 'application'
tags:
  - 'image'
  - 'backend'
  - 'application'
  - 'container'

project:
  name: 'image'
  description: 'Local sd-server (stable-diffusion.cpp) image generation engine via the local-stack compose topology (C-392) — container-only, no Bun source.'
  channel: '#backend'
  owner: 'Backend Team'

dependsOn: []

fileGroups:
  configs:
    - 'package.json'

tasks:
  build:
    command: 'true'

  dev:
    command: 'bun run dev:docker'
    preset: 'server'

  test:
    command: 'bun run test'
    options:
      runInCI: false
