# ============================================================
# Text Microservice — Local LLM Text Generation via Ollama
# Uses the official ollama/ollama image.
# Internally serves on port 11434; Docker maps to host 11436.
# ============================================================

FROM ollama/ollama
EXPOSE 11434
