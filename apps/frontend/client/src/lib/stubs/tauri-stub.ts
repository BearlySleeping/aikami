// Tauri native module stub — resolves @tauri-apps/* imports in the browser.
// These modules are only ever imported dynamically inside __TAURI__-guarded
// code paths, so the stub is never actually used at runtime.
export default {};
