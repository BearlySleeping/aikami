// apps/frontend/client/src/lib/types/vendor.ts
//
// Client-local types for the vendor/crafting overlay.

/** Options passed when opening a vendor session overlay. */
export type VendorSessionOptions = {
  vendorId: string;
  vendorName: string;
  vendorInventory: string;
};
