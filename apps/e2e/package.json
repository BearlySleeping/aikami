{
  "name": "@aikami/e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "test:pwa": "npx playwright test --project=pwa",
    "test:game": "npx playwright test --project=game",
    "test:visual": "bunx playwright test --project=pwa-visual",
    "lpc:smoke": "bun run scripts/lpc_smoke.ts",
    "test:ai-services": "npx playwright test --project=ai-services",
    "lint": "biome lint tests",
    "format": "biome format tests",
    "fix": "biome check --write tests --error-on-warnings && biome format --write tests",
    "typecheck": "tsgo --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0",
    "playwright": "1.60.0",
    "typescript": "6.0.3"
  },
  "dependencies": {
    "@aikami/constants": "workspace:*",
    "@aikami/types": "workspace:*"
  }
}
